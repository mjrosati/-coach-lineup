Coach Lineup v72 — Balanced 3-Column

Keeps the v71 layout concept:
- players permanently left
- field fixed center
- game menu permanently right
- both sidebars scroll independently

Visual cleanup:
- field now fills the available center height
- removes the large black empty area around the field
- better left/right panel proportions
- roster text is easier to read
- right menu buttons are larger/readable and simply scroll
- top game controls are slightly more compact
- line bar stays tight to the bottom of the field

No database migration required.

Replace:
index.html
app.js
styles.css
sw.js

Keep config.js unchanged.

Verify:
v72 • BALANCED 3-COLUMN
