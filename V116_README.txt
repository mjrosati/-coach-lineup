Coach Lineup v116 — Field Fix

Fixes the blank expanded Field from v115.

- The real Game Day .main DOM is no longer moved.
- Expanded FIELD raises the existing working .main above the dashboard using CSS only.
- Existing player taps, substitutions, line controls, game-state controls, and render logic stay intact.
- ALL 5 SECTIONS remains visible.
- Football-style yard lines and hash marks remain.
- Side panels are hidden only while FIELD is expanded.

No database migration required.
